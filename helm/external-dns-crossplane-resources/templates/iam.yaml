apiVersion: iam.aws.upbound.io/v1beta1
kind: Role
metadata:
  name: {{.Values.clusterName}}-external-dns
  namespace: {{.ReleaseNamespace}}
  labels:
      {{- include "labels.common" . | nindent 4 }}
spec:
  providerConfigRef:
    name: {{.Values.clusterName}}
  forProvider:
    tags:
      giantswarm.io/cluster: {{.Values.clusterName}}
      sigs.k8s.io/cluster-api-provider-aws/cluster/{{.Values.clusterName}}: owned
    assumeRolePolicy: |
      {
        "Version": "2012-10-17",
        "Statement": [
          {
            "Effect": "Allow",
            "Principal": {
              "Federated": "arn:aws:iam::{{.Values.accountID}}:oidc-provider/irsa.{{.Values.baseDomain}}"
            },
            "Action": "sts:AssumeRoleWithWebIdentity",
            "Condition": {
              "StringEquals": {
                "irsa.{{.Values.baseDomain}}:sub": "system:serviceaccount:*:*external-dns*"
              }
            }
          }
        ]
      }
    inlinePolicy:
      - name: external-dns-policy
        policy: |
          {
            "Version": "2012-10-17",
            "Statement": [
              {
                "Action": "route53:ChangeResourceRecordSets",
                "Resource": [
                  "arn:aws:route53:::hostedzone/*"
                ],
                "Effect": "Allow"
              },
              {
                "Action": [
                  "route53:ListHostedZones",
                  "route53:ListResourceRecordSets",
                  "route53:ListTagsForResource"
                ],
                "Resource": "*",
                "Effect": "Allow"
              }
            ]
          }